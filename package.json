{
  "name": "routes-payroll",
  "version": "1.0.0",
  "description": "Professional payroll system with: - Secure local admin login - Year-month payroll management - All requested payroll fields and formulas - Payslip preview, print/save PDF flow, TXT download - WhatsApp and Messenger share actions - CSV export and JSON backup/restore - PWA support (installable, offline cache)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "start:open": "./start-routes-payroll.command",
    "check": "node --check server.js && node --check datastore.js && node --check app.js && node --check sw.js && node --check reset-password.js && node --check verify-email.js && node --check sync-auth-to-firestore.js && node --check sync-auth-to-firebase-auth.js",
    "sync:firebase-auth": "node sync-auth-to-firestore.js",
    "sync:firebase-login": "node sync-auth-to-firebase-auth.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@google-cloud/firestore": "^8.3.0",
    "bcryptjs": "^3.0.3",
    "express": "^5.2.1",
    "firebase-admin": "^13.7.0",
    "jsonwebtoken": "^9.0.3",
    "nodemailer": "^8.0.4",
    "sqlite3": "^6.0.1"
  }
}
